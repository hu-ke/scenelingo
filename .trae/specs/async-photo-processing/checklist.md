# Checklist

- [x] 后端内存任务存储字典正确维护任务生命周期
- [x] 后端 `asyncio.create_task` 后台处理不阻塞 API 响应
- [x] `POST /api/recognize/async` 接收多张照片并返回任务 ID 列表
- [x] `GET /api/recognize/status/{task_id}` 返回单任务状态和结果
- [x] `POST /api/recognize/status/batch` 批量返回任务状态
- [x] 照片状态正确流转：pending → processing → completed/failed
- [x] 处理失败时记录错误信息，状态变为 failed
- [x] `PhotoItem` 接口包含 `status`、`taskId`、`errorMessage` 字段（前端 + 小程序）
- [x] `ReviewState` 包含 `isSubmitting` 字段（前端 + 小程序）
- [x] `updatePhotoStatus` action 正确根据 taskId 更新照片状态（前端 + 小程序）
- [x] 前端 `api.ts` 包含 `recognizeAsync`、`getRecognitionStatus`、`getRecognitionStatusBatch` 方法
- [x] 小程序 `api.ts` 包含 `recognizeAsync`、`getRecognitionStatus`、`getRecognitionStatusBatch` 方法
- [x] HomePage 选择照片后调用异步提交接口而非同步识别
- [x] 提交成功后跳转到照片状态预览页（ReviewPage 状态网格模式）
- [x] 照片状态预览页展示所有照片缩略图 + 状态标记
- [x] 前端每 2 秒轮询批量状态接口更新各照片状态
- [x] 所有照片完成后自动停止轮询
- [x] 已完成照片可点击进入复习视图（标注图 + 识别结果）
- [x] 处理中照片不可点击
- [x] 失败照片显示错误信息 + 重试按钮
- [x] 保存/跳过流程在异步模式下正常工作
- [x] 旧同步 `recognizeImage` 逻辑已移除且无残留引用
- [x] 小程序 AppContext 同步前端状态模型
- [x] 小程序 api.ts 适配异步识别和状态查询
- [x] 小程序数据层和 API 层已就绪（UI 页面有待后续单独实现）
- [x] 用户提交 5 张照片后，可立即看到所有缩略图且界面可交互（不阻塞）