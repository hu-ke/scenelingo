# Checklist

## IndexedDB Schema 升级
- [x] DB_VERSION 已升级到 2，`onupgradeneeded` 中创建了 `collectionDate` 索引
- [x] 旧数据（无 `collectionDate`）自动归入 `"earlier"` 分组，不报错
- [x] 新保存的照片自动带上当天日期（`YYYY-MM-DD`）
- [x] `getPhotosGroupedByDate` 正确返回按日期分组的数据

## 全局样式
- [x] CSS 变量更新为暖色渐变主题（主色、辅助色、背景色）
- [x] 页面在 375px-480px 宽度的移动端视口下布局正常
- [x] 按钮最小触控尺寸 ≥44px
- [x] 卡片使用大圆角（≥12px）+ 柔和阴影
- [x] 页面过渡和弹入动画正常无卡顿
- [x] 底部 Tab Bar 样式完整，固定在视口底部

## 首页
- [x] 顶部 Header 显示 App 名称和装饰元素
- [x] 统计卡片正确显示学习天数、照片总数、单词累计（新用户均为 0）
- [x] Collection 列表按日期倒序展示，每个卡片显示日期和照片数量
- [x] Collection 卡片可点击展开/收起，展开后显示当天照片网格
- [x] FAB 上传按钮固定在右下角，点击触发文件选择
- [x] 无照片时显示空状态插画 + 鼓励文案 + FAB 呼吸动画
- [x] 照片可选择（checkbox）、可删除、可选择 ≥2 张后进入合并导出

## Tab Bar 导航
- [x] 首页 Tab 和"学习记录"Tab 正常切换
- [x] 进入审核页时 Tab Bar 隐藏
- [x] 进入合并页时 Tab Bar 隐藏
- [x] 从审核/合并页返回首页时 Tab Bar 恢复显示
- [x] Tab 切换有图标缩放 + 颜色过渡动画

## 审核页
- [x] 顶部渐变进度条显示当前进度（已处理/总数）
- [x] 操作按钮为圆角胶囊样式（保存=主色、重新识别=次要、跳过=边框）
- [x] 保存的照片带有 `collectionDate` 字段
- [x] 识别中显示跳动加载动画
- [x] 全部处理完成后展示撒花动画 + 统计信息
- [x] 顶部有返回按钮可退出审核

## 合并导出页
- [x] 页面头部样式匹配新主题
- [x] Canvas 预览区有圆角边框和新主题阴影
- [x] 下载导出按钮为渐变胶囊样式
- [x] 返回按钮正常工作

## 标注组件
- [x] 边界框颜色更新为柔和紫或其他辅助色
- [x] 标签文字加粗，背景有圆角效果
- [x] "未识别到物体"提示样式匹配新主题

## 边界情况
- [x] IndexedDB 旧数据升级后正常显示在"更早的照片"分组中
- [x] 同一日期多次保存，照片正确归入同一 Collection
- [x] 移动端 Safari/Chrome/微信内置浏览器下布局正常
- [x] 弱网环境下识别加载动画正常显示